# Generated by configure.py on 2016-09-05T16:49:10.824414
services:
  coder:
    build: ./pycoder
    container_name: coder
    env_file:
      - ./erasure.env
    environment:
      - sec_measure=confd
      - splitter=ec
      - k=10
      - m=4
      - type=jerasure_rs_cauchy
      - aes=off
    hostname: coder
  proxy:
    build: ./pyproxy
    container_name: proxy
    env_file:
      - ./erasure.env
    hostname: proxy
    ports:
      - 3000:8000
  redis1:
    container_name: redis1
    image: redis
  redis2:
    container_name: redis2
    image: redis
  redis3:
    container_name: redis3
    image: redis
version: "2"
